import Combine from '../Combinations';

describe('should find combinations', ()=>{

  test('should get results', ()=>{

      const result:number[][] = Combine(5,3);
      console.log(result);

  });


})
